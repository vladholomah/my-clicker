{
  "name": "my-clicker",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "cd my-clicker-backend && npm start",
    "heroku-postbuild": "cd my-clicker-backend && npm install",
    "vercel-build": "cd my-clicker-backend && npm install && npm run build"
  }
}